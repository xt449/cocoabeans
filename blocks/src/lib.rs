mod blocks;
mod state;
mod property;
mod block;

// TODO A large portion of this crate should be auto-generated

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
